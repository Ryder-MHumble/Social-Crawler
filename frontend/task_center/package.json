{
  "name": "social-crawler-task-center",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build && node ./scripts/publish-webui.mjs",
    "build:raw": "vue-tsc --noEmit && vite build",
    "publish:webui": "node ./scripts/publish-webui.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^5.4.14",
    "vue-tsc": "^2.2.0"
  }
}
